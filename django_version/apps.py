from django.apps import AppConfig

class DjangoVersionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'django_version'
    verbose_name = 'Poplar School core application'
